# TCADP 插件定义

TCADP 通过 OpenAPI YAML 导入业务能力接口。当前目录未内置业务插件定义；新增能力时按以下约束创建：

1. 每个接口使用一个独立 YAML 文件。
2. 仅声明 TCADP 支持的 GET/POST 方法。
3. 路径必须对应 API Gateway 中已注册的业务路由。
4. 请求需按平台约定携带签名或认证信息。
5. 导入前校验 OpenAPI 语法，并在测试环境验证完整调用链。
