[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v
markers =
    integration: 集成测试（可能涉及外部依赖/真实构建，部分需显式 opt-in 触发）
    slow: 耗时较长的性能/基准测试，默认不执行，需要 -m slow 显式启用
